# Tasks: 04-ripgrep-odoo-local-skill

## 1. Setup
- [x] 1.1 Create directory `internal/assets/overlays/odoo-development-skill/skills/ripgrep-odoo/`. Acceptance: Directory exists.
- [x] 1.2 Initialize `SKILL.md` in the new directory. Acceptance: File exists.

## 2. Implementation
- [x] 2.1 Implement `backend_orm` pattern in `SKILL.md`. Acceptance: Correct `rg` flags and paths for Python logic.
- [x] 2.2 Implement `frontend_owl` pattern in `SKILL.md`. Acceptance: Correct `rg` flags and paths for OWL components.
- [x] 2.3 Implement `spreadsheet` pattern in `SKILL.md`. Acceptance: Correct `rg` flags and paths for o-spreadsheet.
- [x] 2.4 Add monorepo structure documentation to `SKILL.md`. Acceptance: Accurate paths for CE, EE, OCA.

## 3. Skill Registry
- [x] 3.1 Update `.atl/skill-registry.md` with `ripgrep-odoo` entry. Acceptance: Entry appears in the table with correct path.
- [x] 3.2 Add triggers to registry entry. Acceptance: "odoo", "owl", "spreadsheet" keywords present.

## 4. Verification
- [ ] 4.1 Run manual check of the registry table alignment. Acceptance: Table remains valid Markdown.
- [ ] 4.2 Verify `ripgrep-odoo` SKILL.md against Architect-AI skill standard. Acceptance: Headers and structure match standards.
