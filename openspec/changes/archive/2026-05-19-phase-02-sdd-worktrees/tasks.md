# Tasks: Phase 2 - SDD v3: Phase DAG + Circuit Breaker + Result Contract + Apply Continuity


### v2 Tasks (Branch Isolation + Odoo + Semantic Audit)
- [x] `task 1:` Implement `internal/sdd/branch/manager.go` — create, merge (`--ff-only`/`--no-ff`), delete `apply/{change_name}` branches.
- [x] `task 2:` Create `internal/project/odoo/detector.go` — scan for `__manifest__.py`, `requirements.txt`, `.atl/config.yaml`.
- [x] `task 3:` Write unit tests for the Odoo detector (`internal/project/odoo/detector_test.go`) — empty dir, manifest parsing, requirements parsing.
- [x] `task 4:` Document Apply Branch Protocol in `internal/assets/_shared/apply-branch-protocol.md`.
- [x] `task 5:` Document Odoo detection + routing rules in `internal/assets/_shared/odoo-project-detection.md` and `odoo-overlay-routing.md`.
- [x] `task 6:` Document Semantic Audit Protocol in `internal/assets/_shared/semantic-audit-protocol.md` — Step 0 in `sdd-verify`.
- [x] `task 7:` Update `sdd-design` prompt to mandate FMEA Matrix + FSM Diagram + Pre-Test Contract exit gate.
- [x] `task 8:` Introduce `sdd-hotfix` micro-cycle protocol document.
- [x] `task 9:` Run verification scenarios for branch isolation + Odoo detection routing.

### v3 Tasks (Phase DAG + Circuit Breaker + Result Contract + Apply Continuity)
- [x] `task 10:` Implement `internal/sdd/state/writer.go` — `InitialState`, `WriteSddState` (atomic), `ValidateStateYAML`.
- [x] `task 11:` Write tests for state writer (`internal/sdd/state/writer_test.go`) — required fields, atomic write, validation.
- [x] `task 12:` Create `.atl/sdd-state.yaml` template — generated by `sdd-init`, read by all SDD phase agents.
- [x] `task 13:` Document Phase DAG Enforcement Protocol in `internal/assets/_shared/phase-dag-enforcement.md` — mandatory first action of every SDD agent.
- [x] `task 14:` Create `.atl/scripts/validate-result-contract.sh` — JSON schema validator for Result Contract envelope.
- [x] `task 15:` Document Result Contract JSON schema in `internal/assets/_shared/result-contract.md`.
- [x] `task 16:` Document Circuit Breaker Protocol in `internal/assets/_shared/circuit-breaker.md` — max 3 attempts, exit code 2 = ABANDONED.
- [x] `task 17:` Update `sdd-orchestrator` prompt to validate Result Contract after each phase and handle circuit breaker exit code 2.
- [x] `task 18:` Document Apply Continuity Protocol in `internal/assets/_shared/apply-continuity.md` — `.atl/apply-progress.yaml` structure + resume logic.
- [x] `task 19:` Verify tests — Phase DAG blocks out-of-order (Test 6), Result Contract validation (Test 7), Circuit Breaker 3-strike (Test 8), Apply Continuity resume (Test 9).
